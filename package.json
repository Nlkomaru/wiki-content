{
    "name": "wiki-content",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@11.19.0",
    "scripts": {
        "generate": "hagaki generate",
        "dev": "pnpm run generate && wrangler dev",
        "deploy": "pnpm run generate && wrangler deploy"
    },
    "dependencies": {
        "hagaki": "github:Nlkomaru/hagaki#dist"
    },
    "devDependencies": {
        "@types/node": "^25.0.3",
        "typescript": "^5.9.3",
        "wrangler": "^4.55.0"
    }
}
